apiVersion: v1
kind: Service
metadata:
  name: bedrock
  namespace: code-analyzer
spec:
  selector:
    app: bedrock
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP